export class Part {
  
  title: string;
  content: string;
  code: string;

  constructor(title: string, content: string, code: string) {
    this.title = title;
    this.content = content;
    this.code = code;
  }

}
